#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;
const ll maxn = 2505;
ll arr[maxn];
ll dp[maxn][maxn];
int main () {
	scanf ("%lld%lld%lld%lld", &n, &s, &m, &p);
	for (int i = 0; i < n; ++i) {
		scanf ("%lld", &arr[i]);                                                                                                                                                                                                                                                             
	}
	return 0;
}