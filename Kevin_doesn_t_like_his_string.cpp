#include <bits/stdc++.h>
using namespace std;
const int maxn = 1e5 + 5;
int n, k;
char str[maxn];
int main() {
	scanf ("%d%d", &n, &k);
	scanf ("%s", str);
	for (int i = 0; i < n; ++i)
	return 0;
}